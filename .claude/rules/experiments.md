---
paths:
  - "experiments/**"
---
# Experiment conventions
- This code is intentionally naive or broken. Do NOT "fix" it in place.
- To improve an approach, build the corrected version in src/ and add a
  LEARNING_LOG.md entry with before/after numbers.
- Preserve the original so the failure remains reproducible.